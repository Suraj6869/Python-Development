"""
    encapsulation: the bundling of data with the methods that operate on that data, or the restricting of direct access to some of an object's components. This is a means of preventing accidental interference and misuse of the data. In many object-oriented programming languages, encapsulation is achieved through the use of classes, which can contain both data (attributes) and methods (functions) that operate on that data. By using access modifiers (such as private, protected, and public), developers can control the visibility and accessibility of class members, ensuring that the internal state of an object is protected from unintended modifications.
    inheritance: a fundamental object-oriented programming concept that allows a new class (called a child or subclass) to inherit properties and behaviors (attributes and methods) from an existing class (called a parent or superclass). This promotes code reusability and establishes a natural hierarchical relationship between classes. Inheritance can be single (where a subclass inherits from one parent class) or multiple (where a subclass inherits from multiple parent classes). It also supports the concept of method overriding, where a subclass can provide
    polymorphism: the ability of different classes to be treated as instances of the same class through a common interface. It allows objects of different types to be accessed through the same interface, enabling a single function or method to work with objects of various classes. Polymorphism is often achieved through method overriding (where a subclass provides a specific implementation of a method defined in its superclass) and method overloading (where multiple methods have the same name but different parameters).
"""